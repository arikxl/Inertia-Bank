import React from 'react';
import styled from 'styled-components';

const Card = ({ account, amount, text, button, cardImg, active, inactive, check,
    checkDisabled, text1, text2, text3, text4, text5, text6, text7, text8 }) => {
    return (
        <CardStyled>
            
        </CardStyled>
    )
};

const CardStyled = styled.div`
  
`;

export default Card
